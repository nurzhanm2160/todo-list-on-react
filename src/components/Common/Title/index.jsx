import React from 'react';

const Title = ({title}) => {
    return (
        <div className="title">
            <span>
                {title}
            </span>
        </div>
    );
};

export default Title;